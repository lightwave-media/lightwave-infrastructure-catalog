repos:
  - repo: https://github.com/gruntwork-io/pre-commit
    rev: v0.1.28
    hooks:
      - id: terraform-fmt
      - id: terraform-validate
      # - id: terragrunt-hclfmt  # Disabled: modifies files causing CI failures
      # Disabled until CI tools are installed
      # - id: tflint
      # - id: shellcheck  # Disabled: minor quoting warnings not critical for unblocking PR
      # Disabled: Go module is in test/ subdirectory
      # - id: gofmt
      # - id: goimports
      # - id: golint
